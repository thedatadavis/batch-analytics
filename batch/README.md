All computations and compilation happens in the [analytics](./analytics) directory.

Outputs of the build process go into the [site](./site) directory which where, naturally, the site is served from.
