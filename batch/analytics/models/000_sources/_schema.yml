version: 2

version: 2

sources:
  - name: raw
    database: batch
    schema: manual_imports
    tables:
      - name: orders
        description: Base data from orders import
        
      - name: regions
        description: Base data from regions import

      - name: exceptions
        description: Base data from exceptions import